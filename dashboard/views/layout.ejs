<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Discord Bot Dashboard</title>
    <link href="/css/style.css" rel="stylesheet">
    <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>
</head>
<body>
    <div class="page-wrapper">
        <nav class="navbar">
            <div class="navbar-container">
                <div class="navbar-brand">Bot Dashboard</div>
                <div class="navbar-menu">
                    <ul class="navbar-nav">
                        <li><a href="/" class="nav-link <%= title === 'Dashboard' ? 'active' : '' %>">Home</a></li>
                        <li><a href="/stats" class="nav-link <%= title === 'Statistics' ? 'active' : '' %>">Statistics</a></li>
                        <li><a href="/presence" class="nav-link <%= title === 'Presence Management' ? 'active' : '' %>">Presence</a></li>
                        <li><a href="/config" class="nav-link <%= title === 'Configuration' ? 'active' : '' %>">Configuration</a></li>
                    </ul>
                </div>
                <div class="navbar-end">
                    <button class="btn btn-icon" aria-label="Notifications">
                        <svg class="icon" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.857 17.082a23.848 23.848 0 005.454-1.31A8.967 8.967 0 0118 9.75v-.7V9A6 6 0 006 9v.75a8.967 8.967 0 01-2.312 6.022c1.733.64 3.56 1.085 5.455 1.31m5.714 0a24.255 24.255 0 01-5.714 0m5.714 0a3 3 0 11-5.714 0" />
                        </svg>
                    </button>
                    <% if (locals.user) { %>
                        <a href="/auth/logout" class="btn btn-primary">Logout</a>
                    <% } %>
                </div>
                <button class="mobile-menu-button" x-on:click="mobileMenu = !mobileMenu">
                    <svg class="icon" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-16 6h16" />
                    </svg>
                </button>
            </div>
        </nav>

        <main class="container">
            <% if (locals.success && success.length > 0) { %>
                <div class="alert alert-success fade-in">
                    <div class="alert-content">
                        <svg class="icon" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                        </svg>
                        <p><%= success %></p>
                    </div>
                </div>
            <% } %>

            <% if (locals.error && error.length > 0) { %>
                <div class="alert alert-danger fade-in">
                    <div class="alert-content">
                        <svg class="icon" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
                        </svg>
                        <p><%= error %></p>
                    </div>
                </div>
            <% } %>

            <div class="content fade-in">
                <%- body %>
            </div>
        </main>
    </div>

    <script src="/js/main.js"></script>
</body>
</html>